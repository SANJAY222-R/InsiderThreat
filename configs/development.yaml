# =============================================================================
# Development Environment Configuration
# =============================================================================

app:
  debug: true

server:
  host: "127.0.0.1"
  port: 8000
  workers: 1
  reload: true

database:
  url: "sqlite:///./data/insider_threat_dev.db"
  echo: true

logging:
  level: "DEBUG"
  console: true
  json_format: false

neo4j:
  enabled: false
  uri: "bolt://localhost:7687"
  user: "neo4j"
  password: "changeme"

cors:
  allow_origins:
    - "http://localhost:5173"
    - "http://localhost:3000"
  allow_credentials: true
